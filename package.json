{
  "name": "ism50",
  "version": "1.0.0",
  "private": true,
  "description": "ism50.com. Fifteen years of crypto read as a diffusion index: what was expanding, what was contracting, and who could tell at the time. Astro, static output, MDX content, deployed to Firebase Hosting.",
  "license": "UNLICENSED",
  "author": "Khaled Hawari",
  "type": "module",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "dev": "npm run sync:person && astro dev",
    "start": "npm run dev",
    "sync:person": "node scripts/sync-canonical-person.mjs",
    "build": "npm run verify:emdash && npm run sync:person && astro build && npm run fix:sitemap-xsl && npm run fix:sitemap-lastmod && npm run verify:links && npm run verify:content && npm run verify:subject && npm run verify:schema",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "tsc --noEmit",
    "verify": "npm run verify:emdash && npm run verify:links && npm run verify:content && npm run verify:subject && npm run verify:schema",
    "verify:emdash": "node scripts/verify-em-dash.mjs",
    "verify:links": "node scripts/verify-links.mjs",
    "verify:content": "node scripts/verify-content.mjs",
    "verify:subject": "node scripts/verify-subject.mjs",
    "verify:schema": "node scripts/verify-schema.mjs",
    "fix:sitemap-xsl": "node scripts/fix-sitemap-stylesheet.mjs",
    "fix:sitemap-lastmod": "node scripts/sitemap-lastmod.mjs",
    "indexnow": "node scripts/indexnow.mjs",
    "estate:titles": "node scripts/estate-title-check.mjs",
    "publish:scheduled": "bash scripts/publish-scheduled.sh"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.4",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.3",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
